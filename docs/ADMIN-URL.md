# Admin panel URL

The admin panel is not linked in the main navigation. It is available at:

**`/manage`**

Examples:
- http://localhost:5078/manage
- https://yoursite.com/manage

The path `/Admin` no longer works; use `/manage` only.
